<%= label :scorecard_id, report_scorecard_label, class: 'sr-only' %>
<%= 
  select_impact_card_tag(
    :scorecard_id, 
    prompt: "Select #{indefinite_article(Scorecard.model_name.human.downcase)} #{Scorecard.model_name.human.downcase}"
  ) 
%>
