<!DOCTYPE html>
<html lang='en' class="h-full <%= landing_page_body_bg_class_for_current_theme %>">
  <%= render 'head' %>
  <%= content_for :analytics %>
  <body class="h-full">
    <div id="flash-notifications" class="fixed top-4 right-4 z-50 space-y-2">
      <%= render 'flash_notifications' %>
    </div>
    <main>
      <%= yield %>
    </main>
    <%= render 'home/footer' %>
  </body>
</html>
