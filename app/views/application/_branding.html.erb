<div class="flex items-center gap-x-4">
  <%= image_tag brand_image_path, class: logo_class, alt: "#{current_theme_display_name} Logo" %>
  <h1 class="<%= brand_text_class %>"><%= brand_text %></h1>
</div>