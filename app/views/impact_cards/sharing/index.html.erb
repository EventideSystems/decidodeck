<%= render 'expiry_warning' %>
<div class="h-screen w-full flex bg-gray-50 dark:bg-gray-900 relative" data-controller="drawer">
  <div data-drawer-target="content" class="h-full p-6 transition-all duration-300 ">
    <%= render 'impact_cards/summary', impact_card: @scorecard, print_description: true %>
    <%= render 'impact_cards/tabs', impact_card: @scorecard %>
    <%= render 'sharing', impact_card: @scorecard %>
  </div>
  <%= render 'drawer' do %>
    <%= render 'impact_cards/drawer_actions', impact_card: @scorecard %>
  <% end %>
</div>